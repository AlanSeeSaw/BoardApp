import { updateDoc, doc, serverTimestamp, FieldValue } from 'firebase/firestore';
import { db } from '../firebase';
import { Card } from '../types'; // Assuming CardType is in types.ts

export const CardService = {
  // TODO: add card service functions here
  // They are in BoardService for now
}; 